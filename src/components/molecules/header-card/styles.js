import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#272727'
  },
  image: { width: '100%', height: 400 },
  name: { color: 'white', fontSize: 25, marginTop: 10, marginBottom: 20 },
  description: { color: 'white', fontSize: 17, marginBottom: 40 },
  comicsHeader: { color: 'white', fontSize: 25, marginBottom: 10 }
})

export default styles
