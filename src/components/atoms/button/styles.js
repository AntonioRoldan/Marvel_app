import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    marginTop: 20,
    borderRadius: 12,
    alignItems: 'center',
    justifyContent: 'center',
    height: 24,
    flexDirection: 'row'
  }
})
